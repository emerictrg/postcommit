# Justfile for frontend development

dev:
    # Start the Vite development server
    npm run dev

build:
    # Build the frontend for production
    npm run build

preview:
    # Preview the production build locally
    npm run preview

install:
    # Install dependencies
    npm install

